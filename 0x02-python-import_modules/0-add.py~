#!/usr/bin/python3
# import the function add from the file add_0.py
from add_0 import add
# assign the value 1 to a variable called a
a = 1
# assign the value 2 to a variable called b
b = 2
# print the result of the addition using string format and the variables a and b
print("{} + {} = {}".format(a, b, add(a, b)))
